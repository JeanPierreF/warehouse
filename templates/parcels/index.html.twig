{% extends 'base.html.twig' %}

{% block title %}Hello ParcelsController!{% endblock %}

{% block body %}
<div class="container">
    <div class="row">
            <h1>Liste des conditionnements</h1>
            {# <p><a href={{ path('products_add') }} class="btn btn-primary bt-sm">Ajouter</a></p> #}
            <table class="table table-succes table-striped">
                    <thead>
                            <tr>
                                    <th>Id</a></th>
                                    <th>Commande</a></th>
                                    <th>Quantité max/Colis</a></th>
                                    <th>Code Barre</a></th>
                                    <th>Quantité</a></th>
                                    <th scope="col">Action</th>

                            </tr>


                    </thead>
                    {% for parcel in parcels %}
                    <tr>
                            <td>{{ parcel.id }}</td>
                            <td>{{ parcel.getIdOrder().getId() }}</td>
                            <td>{{ parcel.getIdPackagings().getQuantityMax()}}</td>
                            <td>{{ parcel.eancode }}</td>
                            <td>{{ parcel.quantity }}</td>
                            {# <td><a href={{ path('products_edit', { id:parcel.id } )}} class="btn btn-primary bt-sm">Modifier</a></td> #}
                    </tr>
                    {% endfor %}
            </table>
    </div>
</div>
{% endblock %}
